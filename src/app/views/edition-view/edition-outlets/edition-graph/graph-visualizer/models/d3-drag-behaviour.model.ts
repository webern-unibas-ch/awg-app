import { DragBehavior } from 'd3-drag';

/**
 * The D3ZoomBehaviour interface.
 *
 * It represents the zoom behaviour of a d3 element.
 */
export interface D3DragBehaviour extends DragBehavior<any, any, any> {}
